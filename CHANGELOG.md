# 更新记录（Patch Notes）

记录每个开发批次的用户可见变更。项目尚未采用正式版本号，暂按日期归档。
格式参考常见软件/游戏更新公告：**✨ 新增 · 🐛 修复 · ⚡ 改进 · 🧹 清理 · ⚠️ 已知问题**。

---

# 2026-09-19 · 接手开发批次

> **本次规模**：新增 **2 种文件格式** + **6 项功能** ｜ 修复 **15 个缺陷** ｜
> 测试从 **114 项** 增至 **168 项**（全部通过）｜ 无需重新编译，直接启动即可使用

---

## ✨ 新增功能

### 📚 新增 EPUB 电子书格式（导入 / 导出）

- 直接导入 `.epub`，逐章翻译正文，导出仍是**可正常打开的 EPUB**；
- 图片、CSS、字体、章节结构等**非正文内容原样保留**，不破坏原书排版；
- 内容文档以**良构 XHTML** 写回（自闭合标签、保留命名空间），并遵守 EPUB 规范
  （`mimetype` 必须是压缩包第一个条目且不压缩）；
- 也可把 EPUB 源跨格式导出为 Markdown / txt / HTML / Word（便于二次加工）。

### 🎬 新增 SRT 字幕格式（导入 / 导出）

- 一个字幕条目 = 一个翻译段，**序号与时间轴逐字节保留**，不会错位；
- 支持无序号行的文件、`,` 与 `.` 两种毫秒写法、行尾位置参数；
- ASS 覆盖码（`{\an8}`）与 `<i>` 等标签自动保护，不被翻译破坏；
- 双语模式为字幕常见的「原文行 + 译文行」；
- 什么都没翻译时，输出与源文件**逐字节相同**。

### 🌐 按文件翻译，并明确每个文件的进度

- 新增 **「翻译选中文件」**：只翻你选中的那一个文件，不再只能全局翻译；
- 文件列表的状态列改为明确判定：**未翻译 0/12** · **翻译中 5/12** ·
  **已翻译待校对 12/12** · **已校对完成 12/12**（带颜色区分）；
- 开始翻译的预检对话框会写明**本次范围**（全部文件 / 选中的 N 个）。

### 🔍 术语变更影响分析（新增入口，可撤销）

改了术语译名之后，一键找出**还在用旧译名的已译段落**并批量订正：

- 校对编辑器 → **「术语变更影响…」**，表格列出：术语 / 旧候选 → 新候选 /
  文档 / 段号 / 译文片段；
- **全局替换为新候选**（执行前确认，可**撤销**）或**标记重译**（下次翻译重新生成，
  不影响其他段落）；
- 术语在软件里改、或**直接在 Excel 里改 `glossary.csv`**，都能比出差异
  （重开项目、或点「检查术语表外部修改」、或直接打开该对话框即可）。

### 📤 导出体验

- **导出进度**：显示「正在导出 1/2：source/第001章.md」，**可随时取消**
  （已导出的文件保留，未处理的不动）；
- **完成提示**：系统提示音 + 弹窗汇总成功/失败/提示（以前只在窗口底部闪一行字）；
- **同名文件处理**：可选 **覆盖**（默认）/ **保留两者**（自动改名 `xxx(2).md`）/
  **跳过已存在的文件**，并**提前告诉你 target/ 里已有多少个同名文件**。

### 🌏 语言选择改为下拉框

- 「项目设置」和「新建项目」向导不再要求手打 `ja-JP` 这类编码；
- 21 种语言，显示为 **`en-US（英语-美国）`**、`ja-JP（日语-日本）` 这样的可读形式；
- 旧项目里表外的冷门语言代码会保留，不会被悄悄改掉。

### ⚙️ 其它便利

- **记住上次使用的 Provider 与模型** —— 新建项目时自动预选，不用每次重选/重配密钥；
- **术语归纳选项始终可选**：有新文件走增量，没有则「重新归纳全部文件」
  （以前只在首次有未归纳文件时可勾选）。

---

## 🐛 修复的问题

### 严重（数据丢失 / 功能不可用）

| 问题 | 说明 |
|---|---|
| **校对页改完译文，过后自动消失** | 表格内联编辑只改内存、从不写库；现在改动立即落库（并标为「已修改」、清除待复核标记） |
| **重导入改过的一章，整章被要求重译、校对成果也没了** | 重导入先删光本文件旧段、查翻译记忆时又排除了自己，导致复用恒为 0；现在先快照本文件旧译文再重建，**人工确认过的内容与译注音一并保住** |
| **点「编辑标签」直接崩溃** | 少导入一个组件（`NameError`），功能完全不可用 |
| **术语归纳一直「处理中」、无法取消、关掉后没有术语表** | 进度框既无进度也无取消按钮，关窗还会丢弃结果；现在显示「AI 判定中 40/120」、取消**真的会停下**（不再继续消耗 API）、取消后可重跑 |
| **markdown 里的 `<ruby>` 振假名策略完全失效** | md 适配器不认内联 `<ruby>`，标签连同注音被当正文翻译；现在正确识别，drop/keep/translate 三策略全部生效 |
| **markdown 中 drop 策略仍留下 `<ruby>` 标签** | drop 原先只删注音槽、不管标签；现在标签与注音一起清除，**已译的旧项目无需重译，重新导出即可** |

### 其它修复

| 问题 | 说明 |
|---|---|
| **「确认当前文档全部」按键无效** | 文档下拉默认「全部文档」时静默无操作；现在该按钮在「全部文档」下即确认全部，并按选择动态改变按钮文案 |
| **所有操作提示静默丢失** | 提示信号从来没有接收者；现在统一显示在窗口底部状态栏 |
| **新建项目每次都要重选 Provider / 重配 API** | 向导每次都默认第一个 Provider，不记得上次选择；现已记住 |
| **切换页面残留上一页内容** | 页面不绘制自身背景导致旧像素残留；已修复 |
| **向导「连通性测试」对 Anthropic/Gemini 必然失败** | 原代码写死 OpenAI 协议；现按 `base_url` 自动识别协议 |
| **未配置密钥时没有任何提示** | 异常直接冒泡到 Qt 事件循环；现在弹窗指引去设置页配密钥 |
| **OCR 拿不到项目源语言** | 导入时从不传 `src_lang`，日/韩/英图片一律用中英模型识别；已打通（识别精度仍受模型限制，见「已知问题」） |
| **二次导出静默覆盖已润色的译文** | 现在由你选择覆盖 / 另存副本 / 跳过 |
| **振假名基词「粘连」** | 还原 `<ruby>` 时会把基词前面的词一起卷进去（如 `彼の名は一廣` 整串成为基词）；现按源基词长度截短，md / html / epub / docx 四种格式已统一 |
| **SRT（新格式自查发现）** | 「无序号行 + 空字幕条目」会多出一个前导空行、把时间轴挤错位 |

---

## ⚡ 改进

- 导出模式文案简化：`target（仅译文，按原格式）` → **`target（仅译文）`**；
- 术语表快照改为「内容没变就不写重复快照」—— 否则「上一次真实变更」会被挤出
  只比最近两份的对比窗口，出现「明明改过却比不出来」；
- html 适配器的解析/渲染核心抽成可复用方法，EPUB 直接复用，避免两套逻辑漂移。

## 🧹 清理

- **日常使用不再需要编译**：新增 `启动翻译器.bat`（双击启动、无控制台窗口）与
  `调试启动.bat`（带控制台、能看到报错），并在桌面创建了快捷方式；
- 清掉一批死代码：无用的 `import re`、死状态 `NewProjectWizard._data`、
  死参数 `ExportDialog.source_formats`、重复 import、被取代的旧锚定函数
  （四格式统一到 `split_anchor_hint`）、**价格表下线后遗留的 `provider_prices()`
  与 `precheck()` 里解包了却从未使用的 `price_in/price_out`**；
- 测试跑完不再在工作区留下临时目录。

## 📄 文档

- 新增 **`docs/架构导览.md`**：分层结构、模块速查表、完整数据流、六个关键机制、
  数据模型与缓存语义、UI 结构、格式支持章节、接手须知与雷区清单、
  本轮修复记录、以及未使用代码清单；
- 新增 **`AGENT.md`**（一页速览：定位 / 跑起来与**打包命令** / 格式支持 / 使用流程 /
  配置位置 / 改代码前必读 / 已知边界）与本 **`CHANGELOG.md`**；
- **通篇核对并修正 `README.md`**（对着代码逐条验证，改了 8 处）：
  - 设计方案路径原本写成 `../AI翻译软件-软件设计方案.md`（**不存在**）→ 更正为
    `docs/设计文档/…`，并补上增补设计、验收记录、变更记录的索引表；
  - 支持格式补上 **epub / srt**；`source/` 的格式示例同步更新；
  - 页面数量 **7 页 → 6 页**（术语审核实际是对话框，`--selftest` 也断言 6）；
  - OCR 依赖从 `paddleocr paddlepaddle` 更正为 **`rapidocr-onnxruntime`**，
    并说明 winsdk 是可选的系统 OCR 兜底；
  - 补上「**日常使用不需要编译**」与两个启动器的说明；
  - 「振假名」一条补上 **markdown 内联 `<ruby>`** 与 epub 原生还原，
    并说明锚定按源基词长度截短；
  - 「术语」一条补上**校对页「术语变更影响…」入口**与"外部用 Excel 改过也能对比"；
  - 「图片导出后缀强制 .md」更正为**默认 .md、可显式跨格式导出**；
    并补充 epub 链接/目录不翻译、epub 跨格式导出会丢源格式元素两条边界；
  - 全局配置一项写清实际键名（`concurrency` / `context_segments` / `model_cache` /
    `last_provider_id` 等），并说明 `logs/` 目录已建但暂未写入日志。

## 🧪 测试

- 全量测试 **114 → 168 项**，全部通过；
- 新增覆盖：重导入复用本文件译文、按文件范围翻译、归纳进度与取消、
  **外部改术语表后仍能对比变更**、语言下拉、markdown 内联 `<ruby>` 三策略、
  drop 彻底清除 ruby、导出进度与取消、同名文件三策略、
  **EPUB 容器/良构 XHTML/条目保留/可被自己重新解析**、**SRT 往返逐字节相同**。

---

## ⚠️ 已知问题（本次未修复）

1. **EPUB 目录（nav.xhtml / NCX）里章节名不翻译** —— `<a>` 整体作为保护占位符
   （URL 防注入优先）；需要翻译链接文字的话要单独改占位符机制，可另行安排。
2. **日文/韩文图片 OCR 精度不足** —— 需要额外提供该语言的识别模型文件。
3. **`{0}` 与 `{r0}` 在校验时会被视为同类** —— 模型互换两类标记不会被判为可疑译文。
4. 导出目前总是导出**全部文档**，与校对页顶部的文档筛选器无关。
5. 导出在 UI 线程同步执行：进度会刷新、可取消，但导出期间主窗口不能操作。
6. 设计文档承诺但尚未实现：运行日志（`.aitrans/logs/` 只建目录不写入）、
   翻译期间源文件变更快照。

> 完整清单与修复建议见 `docs/架构导览.md` §10.12「仍然开放」。
